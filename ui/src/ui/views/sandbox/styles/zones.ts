import { html } from "lit";

/* Zone styles are now handled inline via base.ts zone-card classes.
   This file is kept for backward compatibility but only contains
   minimal overrides. */
export const zoneStyles = html`
  <style>
    /* Zone styles moved to base.ts as .zone-card components */
    /* This file intentionally minimal — kept as import target */
  </style>
`;
