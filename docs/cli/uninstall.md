---
summary: "CLI reference for `openaeon uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `openaeon uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
openaeon uninstall
openaeon uninstall --all --yes
openaeon uninstall --dry-run
```
